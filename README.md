# arch-setup
just run:

```
sudo sh -c "$(curl -fsSL https://raw.githubusercontent.com/jannis2106/arch-setup/refs/heads/main/setup.sh)"
```


or to get the .zshrc:
```
wget "https://raw.githubusercontent.com/jannis2106/arch-setup/refs/heads/main/.zshrc" -O .zshrc
```
